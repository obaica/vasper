# local environment is set with this file
### Please revise this file and set $HOME/.vasperrc

### mpirun command
MPIRUN="/path/to/mpirun"

### running Software command
VASP="/path/to/vasp541mpi"
LOBSTER="/path/to/lobster"

### POTCAR Database
POT_DIR="/path/to/potcardb_directory"


# You must not revise the environment variables below
VASPER_DIR=$(dirname  $(dirname `which vasper-makefile.zsh`))
MODULE_DIR="$VASPER_DIR/vasper"
TEMPLATE_DIR="$VASPER_DIR/template"
